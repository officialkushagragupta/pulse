# Environments & secrets — each service's own .env (no shared root .env exists).
# Committed *.env.example templates are kept (negation below).
*.env
!*.env.example
.venv/
venv/

# uv
uv.lock.tmp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Knowledge base build artifacts (generated by K1 ingest, owned by the kb service)
services/knowledge-base-mcp-server/data/index/
*.faiss

# Generated Text-to-SQL dataset (assembled from the batch files)
data/text-to-sql-jsonl/dataset.jsonl

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

.claude/