[project]
name = "knowledge-base-mcp-server"
version = "0.1.0"
description = "Pulse knowledge-base-mcp-server: hybrid (FAISS + BM25) retrieval over data/kb."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.8",
    "pydantic-settings>=2.4",
]

[tool.uv]
package = false
